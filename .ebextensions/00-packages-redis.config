commands:
  01_redis:
    command: |
      # 1. install redis
      yum install redis -y

      # 2. start redis
      systemctl enable redis
      systemctl restart redis
      
    ignoreErrors: true   
