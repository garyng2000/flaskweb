container_commands:
  01_fix_scripts:
    command: |
      #make all shell script executable(if comes from windows)
      chmod 0700 -R .ebextensions/scripts/*.sh
      #fix crlf issue(if comes from windows)
      find . -type f -name "*.sh" -exec dos2unix {} \+;